# SwiftPM / Xcode
.build/
.swiftpm/
DerivedData/
*.xcodeproj
Package.resolved

# macOS
.DS_Store

# Stray local logs (e.g. /tmp debug logs) — keep out of the repo
*.log

# Internal planning docs — kept local, not published to the public repo
docs/specs/
docs/BACKLOG.md
